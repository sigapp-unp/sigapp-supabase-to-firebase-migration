# Configuración para migración Supabase -> Firebase

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Firebase Configuration
FIREBASE_SERVICE_ACCOUNT_PATH=./sigapp-dev-firebase-adminsdk.json

# Migration Settings
MIGRATE_SINCE=2025-07-01T00:00:00Z
PAGE_SIZE=500
CONCURRENCY=10
STRUCTURE_MODE=embedded
DRY_RUN=true

# Notes:
# - MIGRATE_SINCE: Solo migrar registros modificados después de esta fecha (opcional)
# - PAGE_SIZE: Número de registros por página (500 es bueno para balancear memoria y velocidad)
# - CONCURRENCY: Número de operaciones concurrentes (10 es seguro para Firestore)
# - STRUCTURE_MODE: 'embedded' (recomendado) o 'subcollections'
# - DRY_RUN: true para pruebas, false para migración real
