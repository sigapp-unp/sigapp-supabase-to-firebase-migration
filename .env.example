# Configuration for Supabase -> Firebase migration

# Supabase configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Firebase configuration
FIREBASE_SERVICE_ACCOUNT_PATH=./sigapp-dev-firebase-adminsdk.json

# Migration settings
MIGRATE_SINCE=2025-07-01T00:00:00Z
PAGE_SIZE=500
CONCURRENCY=10
STRUCTURE_MODE=embedded
DRY_RUN=true

# Notes:
# - MIGRATE_SINCE: Only migrate records updated since this ISO datetime (optional)
# - PAGE_SIZE: Number of records per page (500 is a reasonable balance between memory and speed)
# - CONCURRENCY: Number of concurrent workers (10 is conservative for Firestore)
# - STRUCTURE_MODE: 'embedded' (recommended) or 'subcollections'
# - DRY_RUN: true for testing, false for real migration
